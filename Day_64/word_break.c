#include <stdio.h>
#include <string.h>

int wordBreak(char str[], char dict[][20], int size);

int main()
{
    char str[100] = "leetcode";
    char dict[5][20] = {"leet","code"};

    int n = strlen(str);

    return 0;
}

int wordBreak(char str[], char dict[][20], int size)
{
    return 0;
}