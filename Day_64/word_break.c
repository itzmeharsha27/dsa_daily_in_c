#include <stdio.h>
#include <string.h>

int wordBreak(char str[], char dict[][20], int size);

int main()
{
    char str[100];
    scanf("%s", str);

    return 0;
}

int wordBreak(char str[], char dict[][20], int size)
{
    return 0;
}