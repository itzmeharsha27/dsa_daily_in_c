#include <stdio.h>

int reverse(int n);

int main()
{
    int n, result;

    printf("Enter a number: ");
    scanf("%d", &n);

    result = reverse(n);
    printf("Reversed number = %d\n", result);

    return 0;
}

int reverse(int n)
{
    return 0;
}