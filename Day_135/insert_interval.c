#include <stdio.h>

struct Interval
{
    int start;
    int end;
};

int main()
{
    struct Interval arr[] = {{1,3},{6,9}};
    struct Interval newInterval = {2,5};
    int n = 2;

    int i = 0;

    return 0;
}