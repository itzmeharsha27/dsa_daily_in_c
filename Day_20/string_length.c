#include <stdio.h>

int main()
{
    char str[100];
    int i;

    printf("Enter a string: ");
    scanf("%s", str);

    // Calculate string length
    for(i=0; str[i] != '\0'; i++);
    {
        
    }
    return 0;
}