#include <stdio.h>

int adj[100][100];
int visited[100];
int V = 5;

int dfs(int node, int parent)
{
    visited[node] = 1;

    for(int i=0;i<V;i++)
    {
    }

    return 0;
}

int main()
{
    return 0;
}