#include <stdio.h>

int isSorted(int arr[], int n);

int main()
{
    int n;

    printf("Enter number of elements: ");
    scanf("%d", &n);

    return 0;
}

int isSorted(int arr[], int n)
{
    return 1;
}