#include <stdio.h>

int dfs(int graph[10][10], int visited[], int node, int parent, int n);

int main()
{
    int n;

    printf("Enter number of vertices: ");
    scanf("%d", &n);

    return 0;
}

int dfs(int graph[10][10], int visited[], int node, int parent, int n)
{
    return 0;
}