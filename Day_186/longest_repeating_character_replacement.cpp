#include <iostream>
#include <string>
#include <vector>
using namespace std;

int characterReplacement(string s, int k)
{
    vector<int> freq(26, 0);

    int left = 0;
    int maxFreq = 0;
    int answer = 0;

    return answer;
}

int main()
{
    string s = "AABABBA";
    int k = 1;

    cout << characterReplacement(s, k);

    return 0;
}