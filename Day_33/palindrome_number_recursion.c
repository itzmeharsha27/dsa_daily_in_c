#include <stdio.h>

int reverse(int n, int rev);

int main()
{
    int n, revNum;

    printf("Enter a number: ");
    scanf("%d", &n);

    revNum = reverse(n, 0);

    return 0;
}

int reverse(int n, int rev)
{
    return 0;
}