#include <stdio.h>
#include <string.h>

#define MAX 100

int main()
{
    char s1[] = "horse";
    char s2[] = "ros";

    int dp[MAX][MAX];

    int n = strlen(s1);
    int m = strlen(s2);

    return 0;
}