#include <stdio.h>

int powerFunc(int base, int power);

int main()
{
    int base, power;

    printf("Enter base and power: ");
    scanf("%d %d", &base, &power);

    return 0;
}

int powerFunc(int base, int power)
{
    return 0;
}