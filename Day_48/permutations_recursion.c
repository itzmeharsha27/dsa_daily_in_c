#include <stdio.h>
#include <string.h>

void permute(char str[], int l, int r);

int main()
{
    char str[100];
    int len;

    printf("Enter a string: ");
    scanf("%s", str);

    len = strlen(str);

    return 0;
}

void permute(char str[], int l, int r)
{
}