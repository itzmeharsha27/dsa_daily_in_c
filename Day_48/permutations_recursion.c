#include <stdio.h>

void permute(char str[], int l, int r);

int main()
{
    char str[100];

    printf("Enter a string: ");
    scanf("%s", str);

    return 0;
}

void permute(char str[], int l, int r)
{
}