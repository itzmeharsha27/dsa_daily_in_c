#include <stdio.h>
#include <string.h>

#define MAX 100

int main()
{
    char s1[] = "abcde";
    char s2[] = "abfce";

    int dp[MAX][MAX] = {0};

    int n = strlen(s1);
    int m = strlen(s2);

    return 0;
}