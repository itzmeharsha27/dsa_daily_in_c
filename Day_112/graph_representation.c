#include <stdio.h>
#include <stdlib.h>

// Graph Representation

#define MAX 100

struct Node {
    int data;
    struct Node* next;
};

int main()
{
    int V = 4;

    struct Node* adjList[MAX];

    return 0;
}