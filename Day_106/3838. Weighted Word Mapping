#include <stdio.h>
#include <string.h>

#define MAX 100

int main()
{
    char exp[] = "T|F&T";

    int Tdp[MAX][MAX] = {0};
    int Fdp[MAX][MAX] = {0};

    int n = strlen(exp);

    for(int i=0;i<n;i+=2)
    {
        if(exp[i] == 'T')
            Tdp[i][i] = 1;
        else
            Fdp[i][i] = 1;
    }

    for(int len=3; len<=n; len+=2)
    {
    }

    return 0;
}