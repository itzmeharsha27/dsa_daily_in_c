#include <stdio.h>

int lastOcc(int arr[], int n, int key, int index);

int main()
{
    int n;

    printf("Enter number of elements: ");
    scanf("%d", &n);

    return 0;
}

int lastOcc(int arr[], int n, int key, int index)
{
    return -1;
}