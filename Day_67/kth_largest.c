#include <stdio.h>
#include <stdlib.h>

#define MAX 100

struct Heap
{
    int arr[MAX];
    int size;
};

int kthLargest(int arr[], int n, int k);

int main()
{
    return 0;
}

int kthLargest(int arr[], int n, int k)
{
    return 0;
}