#include <stdio.h>
#include <stdlib.h>

#define MAX 100

struct Heap
{
    int arr[MAX];
    int size;
};

void heapifyDown(struct Heap* h, int i);

int main()
{
    return 0;
}

void heapifyDown(struct Heap* h, int i)
{
}